name: lighthouse-ci

on: [push]

jobs:
  execute-job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout step
        uses: actions/checkout@v4
      - name: execute-step
        run: |
          npm install -g @lhci/cli@0.13.x
          lhci autorun
